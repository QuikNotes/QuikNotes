name: Build and Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm run test

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
    - name: Deploy to Render
      run: |
        echo "Triggering deployment to Render..."
        # Uncomment and update the following line with your actual Render deploy webhook
        curl -X POST "https://api.render.com/deploy/srv-d0jogbi4d50c73fhpq60?key=HFZHeGFkCHk"
